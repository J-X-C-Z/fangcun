<template>
  <div class="page" @swipe="handleSwipe">
    <scroll class="page-scroll" scroll-y="true" bounces="true">
      <div class="content column">
        <div class="header column">
          <text class="eyebrow">方寸</text>
          <text class="title">更多</text>
        </div>
        <div class="status row">
          <div class="status-indicator"></div>
          <div class="status-copy column">
            <text class="status-title">等待连接手机</text>
            <text class="status-note">当前使用静态测试数据</text>
          </div>
        </div>
        <div class="info column">
          <div class="info-row row"><text class="info-label">本地缓存</text><text class="info-value">Mock</text></div>
          <div class="info-row row"><text class="info-label">最近同步</text><text class="info-value">尚未同步</text></div>
          <div class="info-row row"><text class="info-label">应用版本</text><text class="info-value">0.1.0</text></div>
        </div>
        <text class="gesture-hint">右滑返回功能菜单</text>
      </div>
    </scroll>
  </div>
</template>

<script>
import router from "@system.router"
import haptic from "../../common/haptic"

export default {
  handleSwipe(e) {
    if (e.direction === "right") { haptic.light(); router.back() }
  }
}
</script>

<style>
text { color: #ffffff; }
.row { flex-direction: row; }
.column { flex-direction: column; }
.page { width: 100%; height: 100%; padding: 24px; border-radius: 36px; background-color: #000000; }
.page-scroll { width: 100%; height: 100%; border-radius: 24px; }
.content { width: 288px; padding-bottom: 28px; }
.header { width: 288px; height: 84px; }
.eyebrow { font-size: 18px; color: #a0a0a0; }
.title { margin-top: 4px; font-size: 42px; font-weight: 700; color: #ffffff; }
.status { width: 288px; height: 102px; padding: 18px; align-items: center; border-radius: 18px; background-color: #1b2724; }
.status-indicator { width: 14px; height: 14px; margin-right: 16px; border-radius: 7px; background-color: #c7e755; }
.status-copy { width: 222px; }
.status-title { font-size: 22px; font-weight: 700; color: #ffffff; }
.status-note { margin-top: 6px; font-size: 16px; color: #b0b9b4; }
.info { width: 288px; margin-top: 20px; }
.info-row { width: 288px; height: 68px; justify-content: space-between; align-items: center; border-bottom: 1px solid #252525; }
.info-label { font-size: 21px; color: #ffffff; }
.info-value { font-size: 17px; color: #a0a0a0; }
.gesture-hint { width: 288px; margin-top: 30px; font-size: 16px; text-align: center; color: #858585; }
</style>
