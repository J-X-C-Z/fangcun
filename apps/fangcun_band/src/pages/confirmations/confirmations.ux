<template>
  <div class="page column {{motionClass}}" @swipe="handleSwipe">
    <div class="top-bar row"><text class="back" onclick="goBack">‹</text><text class="page-title">待确认</text><text class="clock">19:06</text></div>

    <div class="confirm-card column" if="{{status === 'pending'}}">
      <div class="card-head row"><div class="confirm-icon center"><div class="glyph check-glyph"><div class="check-stroke"></div></div></div><text class="source">智能识别</text><div class="badge center"><text class="badge-text">1/2</text></div></div>
      <text class="label">识别到任务</text>
      <text class="task-title">{{task.title}}</text>
      <text class="task-time">{{task.note}}</text>
      <div class="actions row">
        <input class="button secondary" type="button" value="忽略" onclick="ignore" />
        <input class="button primary" type="button" value="确认" onclick="confirm" />
      </div>
    </div>

    <div class="result column center" if="{{status !== 'pending'}}">
      <div class="result-icon center"><div class="glyph result-glyph"><div class="result-stroke {{status === 'confirmed' ? 'result-check-stroke' : 'result-dash-stroke'}}"></div></div></div>
      <text class="result-title">{{status === 'confirmed' ? '已确认' : '已忽略'}}</text>
      <text class="result-note">操作将在连接手机后同步</text>
    </div>
    <text class="hint">右滑返回功能菜单</text>
  </div>
</template>

<script>
import router from "@system.router"
import haptic from "../../common/haptic"
import link from "../../utils/interconnect.js"
import { snapshotTasks } from "../../utils/snapshot.js"
export default {
  private: { status: "pending", motionClass: "enter-right", task: { id: "", title: "完成大学物理作业", due: "明天 23:59" } },
  onShow() {
    const tasks = snapshotTasks(link.getSnapshot(), 20).filter(task => !task.completed)
    if (tasks.length) this.task = tasks[0]
    this.status = "pending"
  },
  sendAction(action) {
    link.sendAction({ action: action, taskId: this.task.id, at: new Date().toISOString() }).catch(() => {})
  },
  confirm() { haptic.medium(); this.status = "confirmed"; this.sendAction("confirm") },
  ignore() { haptic.light(); this.status = "ignored"; this.sendAction("ignore") },
  goBack() { haptic.light(); router.back() },
  handleSwipe(e) {
    if (e.direction === "right") {
      haptic.light()
      this.motionClass = "exit-right"
      setTimeout(() => router.back(), 170)
    }
  }
}
</script>

<style>
text { color: #ffffff; }
.row { flex-direction: row; }
.column { flex-direction: column; }
.center { justify-content: center; align-items: center; }
.page { width: 100%; height: 100%; padding: 18px; border-radius: 36px; background-color: #000000; }
.enter-right { animation-name: enterRight; animation-duration: 180ms; animation-timing-function: cubic-bezier(0.23, 1, 0.32, 1); }
.exit-right { animation-name: exitRight; animation-duration: 180ms; animation-timing-function: cubic-bezier(0.23, 1, 0.32, 1); }
.top-bar { width: 300px; height: 56px; align-items: center; }
.back { width: 34px; font-size: 40px; color: #ffffff; }
.page-title { width: 192px; font-size: 27px; font-weight: 700; color: #ffffff; }
.clock { width: 74px; font-size: 21px; text-align: right; color: #e3e6ec; }
.confirm-card { width: 300px; padding: 18px; border-radius: 18px; background-color: #191d22; }
.card-head { width: 264px; height: 46px; align-items: center; }
.confirm-icon { width: 44px; height: 44px; margin-right: 12px; border-radius: 22px; background-color: #146769; }
.glyph { width: 18px; height: 18px; position: relative; }
.check-glyph { width: 15px; height: 8px; }
.check-stroke { width: 15px; height: 8px; border-left: 3px solid #ffffff; border-bottom: 3px solid #ffffff; transform: rotate(-45deg); }
.source { width: 158px; font-size: 21px; color: #aab2bf; }
.badge { width: 52px; height: 32px; border-radius: 16px; background-color: #2d66ad; }
.badge-text { font-size: 16px; color: #ffffff; }
.label { margin-top: 24px; font-size: 17px; color: #9aa3b1; }
.task-title { margin-top: 9px; font-size: 29px; font-weight: 700; color: #ffffff; }
.task-time { margin-top: 14px; font-size: 21px; color: #aeb7c5; }
.actions { width: 264px; margin-top: 30px; justify-content: space-between; }
.button { width: 124px; height: 62px; border-radius: 18px; font-size: 22px; }
.secondary { background-color: #30353c; color: #eef1f6; }
.primary { background-color: #2878f2; color: #ffffff; }
.result { width: 300px; height: 320px; }
.result-icon { width: 74px; height: 74px; border-radius: 37px; background-color: #35d586; }
.result-glyph { width: 32px; height: 32px; }
.result-stroke { position: absolute; left: 5px; top: 14px; width: 23px; height: 4px; background-color: #ffffff; }
.result-check-stroke { width: 21px; height: 12px; left: 5px; top: 7px; background-color: transparent; border-left: 5px solid #ffffff; border-bottom: 5px solid #ffffff; transform: rotate(-45deg); }
.result-title { margin-top: 20px; font-size: 32px; font-weight: 700; color: #ffffff; }
.result-note { margin-top: 13px; font-size: 17px; color: #939cab; }
.hint { width: 300px; margin-top: 25px; font-size: 16px; text-align: center; color: #7a8391; }
@keyframes enterRight { 0% { transform: translateX(32px); opacity: 0.2; } 100% { transform: translateX(0px); opacity: 1; } }
@keyframes exitRight { 0% { transform: translateX(0px); opacity: 1; } 100% { transform: translateX(32px); opacity: 0.2; } }
</style>
