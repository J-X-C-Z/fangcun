<template>
  <div class="page {{motionClass}}" @swipe="handleSwipe">
    <scroll class="page-scroll" scroll-y="true" bounces="true">
      <div class="content column">
        <div class="top-bar row"><text class="back" onclick="goBack">‹</text><text class="page-title">设置</text><text class="clock">19:06</text></div>

        <div class="connection row">
          <div class="connection-dot"></div>
          <div class="column"><text class="connection-title">数据同步</text><text class="connection-note">{{connectionNote}}</text></div>
        </div>
        <text class="diagnosis-note">{{diagnosisNote}}</text>

        <div class="setting row">
          <div class="setting-icon amber center"><div class="glyph vibration-glyph"><div class="vibe-short"></div><div class="vibe-long"></div></div></div>
          <text class="setting-title">振动提醒</text>
          <switch checked="true"></switch>
        </div>
        <div class="setting row">
          <div class="setting-icon blue center"><div class="glyph message-glyph"><div class="message-tail"></div></div></div>
          <text class="setting-title">消息提醒</text>
          <switch checked="true"></switch>
        </div>
        <div class="setting row">
          <div class="setting-icon slate center"><div class="glyph gear-glyph"><div class="gear-core"></div></div></div>
          <text class="setting-title">关于方寸</text>
          <text class="chevron">›</text>
        </div>

        <input class="exit-button" type="button" value="强制退出" onclick="forceExit" />

        <text class="sync-note">账号与同步服务请在手机版管理 · 当前为只读预览</text>
        <text class="hint">右滑返回功能菜单</text>
      </div>
    </scroll>
  </div>
</template>

<script>
import router from "@system.router"
import app from "@system.app"
import haptic from "../../common/haptic"
import link from "../../utils/interconnect.js"
export default {
  private: { motionClass: "enter-right", connectionNote: "fangcun.link.v1 · 等待手机连接", diagnosisNote: "连接诊断待运行" },
  onShow() {
    this.updateConnection(link.getState())
    this.stopLinkListener = link.on((event, state) => {
      if (event === "state") this.updateConnection(state)
    })
    link.refreshState()
    this.diagnosisNote = "正在检查连接…"
    link.diagnose().then(result => {
      const notes = { 0: "诊断：连接正常", 204: "诊断：连接超时", 1001: "诊断：手机应用未安装", 1000: "诊断：连接失败" }
      this.diagnosisNote = notes[result.status] || "诊断：未知状态"
    }).catch(() => { this.diagnosisNote = "诊断：检查失败" })
  },
  onHide() {
    if (this.stopLinkListener) this.stopLinkListener()
    this.stopLinkListener = null
  },
  updateConnection(state) {
    this.connectionNote = state === "connected" ? "fangcun.link.v1 · 手机已连接" : "fangcun.link.v1 · 等待手机连接"
  },
  goBack() { haptic.light(); router.back() },
  forceExit() { haptic.heavy(); app.terminate() },
  handleSwipe(e) {
    if (e.direction === "right") {
      haptic.light()
      this.motionClass = "exit-right"
      setTimeout(() => router.back(), 170)
    }
  }
}
</script>

<style>
text { color: #ffffff; }
.row { flex-direction: row; }
.column { flex-direction: column; }
.center { justify-content: center; align-items: center; }
.page { width: 100%; height: 100%; padding: 18px; border-radius: 36px; background-color: #000000; }
.enter-right { animation-name: enterRight; animation-duration: 180ms; animation-timing-function: cubic-bezier(0.23, 1, 0.32, 1); }
.exit-right { animation-name: exitRight; animation-duration: 180ms; animation-timing-function: cubic-bezier(0.23, 1, 0.32, 1); }
.page-scroll { width: 100%; height: 100%; border-radius: 24px; }
.content { width: 300px; padding-bottom: 20px; }
.top-bar { width: 300px; height: 56px; align-items: center; }
.back { width: 34px; font-size: 40px; color: #ffffff; }
.page-title { width: 192px; font-size: 27px; font-weight: 700; color: #ffffff; }
.clock { width: 74px; font-size: 21px; text-align: right; color: #e3e6ec; }
.connection { width: 300px; height: 88px; margin-bottom: 14px; padding: 16px; align-items: center; border-radius: 18px; background-color: #17241f; }
.connection-dot { width: 14px; height: 14px; margin-right: 14px; border-radius: 7px; background-color: #35d586; }
.connection-title { font-size: 22px; font-weight: 700; color: #ffffff; }
.connection-note { margin-top: 5px; font-size: 16px; color: #8e9d95; }
.diagnosis-note { width: 300px; margin-bottom: 10px; font-size: 16px; color: #8e9d95; }
.setting { width: 300px; height: 84px; margin-bottom: 10px; padding-left: 14px; padding-right: 14px; align-items: center; border-radius: 18px; background-color: #1b1f24; }
.setting-icon { width: 44px; height: 44px; margin-right: 14px; border-radius: 22px; }
.amber { background-color: #a96a18; }
.blue { background-color: #285f9f; }
.slate { background-color: #58657b; }
.glyph { width: 22px; height: 22px; position: relative; }
.vibe-short { position: absolute; left: 8px; top: 4px; width: 4px; height: 12px; border-left: 2px solid #ffffff; border-right: 2px solid #ffffff; transform: rotate(15deg); }
.vibe-long { position: absolute; left: 2px; top: 2px; width: 4px; height: 16px; border-left: 2px solid #ffffff; border-right: 2px solid #ffffff; transform: rotate(15deg); }
.message-glyph { border: 2px solid #ffffff; border-radius: 4px; }
.message-tail { position: absolute; left: 3px; bottom: -5px; width: 6px; height: 6px; border-left: 2px solid #ffffff; transform: skewY(-35deg); }
.gear-glyph { border: 3px dotted #ffffff; border-radius: 50%; }
.gear-core { position: absolute; left: 6px; top: 6px; width: 6px; height: 6px; border-radius: 50%; background-color: #ffffff; }
.setting-title { width: 174px; font-size: 22px; font-weight: 700; color: #ffffff; }
switch { width: 56px; height: 34px; }
.chevron { width: 54px; font-size: 34px; text-align: right; color: #a5adba; }
.exit-button { width: 300px; height: 64px; margin-top: 12px; border-radius: 18px; background-color: #713c42; font-size: 21px; font-weight: 700; color: #ffd9dc; }
.sync-note { width: 300px; margin-top: 12px; font-size: 16px; text-align: center; color: #8a94a3; }
.hint { width: 300px; margin-top: 20px; font-size: 16px; text-align: center; color: #7a8391; }
@keyframes enterRight { 0% { transform: translateX(32px); opacity: 0.2; } 100% { transform: translateX(0px); opacity: 1; } }
@keyframes exitRight { 0% { transform: translateX(0px); opacity: 1; } 100% { transform: translateX(32px); opacity: 0.2; } }
</style>
